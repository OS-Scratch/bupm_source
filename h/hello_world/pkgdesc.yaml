# Under pkgdesc.yaml, the package manager gets the info about the package.
# The package category contains info about the package. Name, version and description.
package:
    name: hello_world
    version: 1.0.1
    desc: "A dummy package for testing if bupm works."
dependencies:
    direct:
    pulled:
system:
    owner:
    custom-root:
    triggers:
    license:
security:
    requires: none
    deprecated: no
    permission: none
